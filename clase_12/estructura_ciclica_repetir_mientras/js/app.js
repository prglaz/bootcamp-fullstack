let contadorA = 1;

while (contadorA < 10) {
    document.write("<p>Iteracion A " + contadorA + "</p>");
    //console.log("Iteracion " + contador);
    contadorA++; //incrementa a contador en 1
    //contador += 2; //es igual a: contador = contador + 2;
    //break;
}

let contadorB = 0;
while (contadorB <= 5) {
    document.write("<p>Iteracion B " + contadorB + "</p>");
    contadorB += 2; //incrementando en 2 (contadorB = contadorB + 2;)
}
document.write("<p>Fin de instrucciones</p>");
